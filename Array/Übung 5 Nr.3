using System;

namespace Übung_5
{
    class Program
    {
        static void Main(string[] args)
        {
            //Nr.3 
            Console.WriteLine("Bitte geben sie ein Wort ein");
            string Wort = Console.ReadLine().ToLower();

            char[] wortArray = Wort.ToCharArray();
            Array.Reverse(wortArray);
            string umgetretesWort = new string(wortArray);
            if (Wort == umgetretesWort)
            {
                Console.WriteLine("Das ist ein Palidrom");
            }
            else
            {
                Console.WriteLine("Das ist kein Palidrom");
            }    
        }

    }
    
}
