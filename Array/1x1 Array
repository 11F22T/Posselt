using System;

public class Program
{
    public static void Main()
    {
        int[,] array1 = new int[10, 10];
        int[,] array2 = new int[10, 10];
        int[,] array3 = new int[10, 10];

        // Kleines Einmaleins von 1 bis 100
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                array1[i, j] = (i + 1) * (j + 1);
            }
        }

        // Neues Muster für array2
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                array2[i, j] = (10 - i) * (10 - j);
            }
        }

        // Füllen des dritten Arrays
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                array3[i, j] = array1[i, j] * array2[i, j];
            }
        }

        // Ausgabe von array1
        Console.WriteLine("Array 1:");
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                Console.Write("{0,3} ", array1[i, j]);
            }
            Console.WriteLine();
        }

        // Ausgabe von array2
        Console.WriteLine("\nArray 2:");
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                Console.Write("{0,3} ", array2[i, j]);
            }
            Console.WriteLine();
        }

        // Ausgabe von array3
        Console.WriteLine("\nArray 3:");
        for (int i = 0; i < 10; i++)
        {
            for (int j = 0; j < 10; j++)
            {
                Console.Write("{0,3} ", array3[i, j]);
            }
            Console.WriteLine();
        }
    }
}
